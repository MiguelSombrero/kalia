spring.application.name=kalia

# Local development database (docker-compose.yml); tests override via Testcontainers
spring.datasource.url=jdbc:postgresql://localhost:5432/kalia
spring.datasource.username=kalia
spring.datasource.password=${POSTGRES_PASSWORD:kalia}

# Flyway owns the schema; module-specific locations are added as modules land
spring.flyway.locations=classpath:db/migration/common
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
